# Gitea - WACS UI customizations

This project is UI customizations for WA's Gitea instance.

## Add'l Notes
- The qr code in footer is built from https://github.com/soldair/node-qrcode. Just build the lib and vendored in. 